const mazesAndPatternsOptions = () => {
  const mazesAndPatterns = [
    { key: 'normal', text: 'Normal', value: 'normal' },
    { key: 'maze one', text: 'maze one', value: 'maze one' },
  ];

  return mazesAndPatterns;
};

export default mazesAndPatternsOptions;
